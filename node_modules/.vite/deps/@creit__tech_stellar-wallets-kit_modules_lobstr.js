import {
  ModuleType,
  parseError
} from "./chunk-O2MVKXLV.js";
import {
  __commonJS,
  __toESM
} from "./chunk-KEXKKQVW.js";

// node_modules/@lobstrco/signer-extension-api/build/index.min.js
var require_index_min = __commonJS({
  "node_modules/@lobstrco/signer-extension-api/build/index.min.js"(exports, module) {
    !function(e, n) {
      "object" == typeof exports && "object" == typeof module ? module.exports = n() : "function" == typeof define && define.amd ? define([], n) : "object" == typeof exports ? exports.lobstrExtensionApi = n() : e.lobstrExtensionApi = n();
    }(exports, () => (() => {
      "use strict";
      var e = { d: (n2, o2) => {
        for (var t2 in o2) e.o(o2, t2) && !e.o(n2, t2) && Object.defineProperty(n2, t2, { enumerable: true, get: o2[t2] });
      }, o: (e2, n2) => Object.prototype.hasOwnProperty.call(e2, n2), r: (e2) => {
        "undefined" != typeof Symbol && Symbol.toStringTag && Object.defineProperty(e2, Symbol.toStringTag, { value: "Module" }), Object.defineProperty(e2, "__esModule", { value: true });
      } }, n = {};
      e.r(n), e.d(n, { default: () => u, getPublicKey: () => a, isBrowser: () => S, isConnected: () => c, signMessage: () => l, signTransaction: () => d });
      let o = function(e2) {
        return e2[e2.V1 = 0] = "V1", e2[e2.V2 = 1] = "V2", e2;
      }({}), t = function(e2) {
        return e2.REQUEST_ACCESS = "REQUEST_ACCESS", e2.SIGN = "SIGN", e2.SUBMIT_TRANSACTION = "SUBMIT_TRANSACTION", e2.SIGN_TRANSACTION = "SIGN_TRANSACTION", e2.REQUEST_CONNECTION_STATUS = "REQUEST_CONNECTION_STATUS", e2;
      }({});
      const r = "LOBSTR_CONNECTION_KEY", i = (e2) => {
        const n2 = Date.now() + Math.random();
        return window.postMessage({ source: "LOBSTR_EXTERNAL_MSG_REQUEST", messageId: n2, ...e2 }, window.location.origin), new Promise((o2) => {
          let r2 = 0;
          e2.type === t.REQUEST_CONNECTION_STATUS && (r2 = setTimeout(() => {
            o2({ isConnected: false }), window.removeEventListener("message", i2);
          }, 2e3));
          const i2 = (e3) => {
            var t2, s2;
            e3.source === window && "LOBSTR_EXTERNAL_MSG_RESPONSE" === (null == e3 || null === (t2 = e3.data) || void 0 === t2 ? void 0 : t2.source) && (null == e3 || null === (s2 = e3.data) || void 0 === s2 ? void 0 : s2.messagedId) === n2 && (o2(e3.data), window.removeEventListener("message", i2), clearTimeout(r2));
          };
          window.addEventListener("message", i2, false);
        });
      }, s = async (e2, n2, r2) => {
        let s2 = { signedData: "", error: "", signerAddress: "" };
        try {
          s2 = await i({ dataToSign: e2, connectionKey: n2, signType: r2, type: t.SIGN, version: o.V2 });
        } catch (e3) {
          throw console.error(e3), e3;
        }
        if (s2.error) throw s2.error;
        return s2;
      }, a = async () => {
        if (!S) return "";
        const { publicKey: e2, connectionKey: n2 } = await (async () => {
          let e3 = { publicKey: "", error: "", connectionKey: "" };
          try {
            e3 = await i({ type: t.REQUEST_ACCESS, version: o.V2 });
          } catch (e4) {
            console.error(e4);
          }
          const { publicKey: n3, connectionKey: r2, error: s2 } = e3;
          if (s2) throw s2;
          return { publicKey: n3, connectionKey: r2 };
        })();
        return ((e3) => {
          var n3, o2;
          null === (n3 = window) || void 0 === n3 || null === (o2 = n3.sessionStorage) || void 0 === o2 || o2.setItem(r, e3);
        })(n2), e2;
      }, d = async (e2) => {
        if (!S) return Promise.resolve("");
        const n2 = (null === (o2 = window) || void 0 === o2 || null === (t2 = o2.sessionStorage) || void 0 === t2 ? void 0 : t2.getItem(r)) || "";
        var o2, t2;
        return (await s(e2, n2, "transaction")).signedData;
      }, c = async () => !!S && (window.lobstrSignerExtension ? window.lobstrSignerExtension : await (async () => {
        let e2 = { isConnected: false };
        try {
          e2 = await i({ type: t.REQUEST_CONNECTION_STATUS, version: o.V2 });
        } catch (e3) {
          console.error(e3);
        }
        return e2.isConnected;
      })()), l = async (e2) => {
        if (!S) return Promise.resolve(null);
        const n2 = (null === (o2 = window) || void 0 === o2 || null === (t2 = o2.sessionStorage) || void 0 === t2 ? void 0 : t2.getItem(r)) || "";
        var o2, t2;
        const i2 = await s(e2, n2, "message");
        return { signedMessage: i2.signedData, signerAddress: i2.signerAddress };
      }, S = "undefined" != typeof window, u = { getPublicKey: a, signTransaction: d, isConnected: c, signMessage: l };
      return n;
    })());
  }
});

// node_modules/@creit.tech/stellar-wallets-kit/esm/sdk/modules/lobstr.module.js
var import_signer_extension_api = __toESM(require_index_min());
var LOBSTR_ID = "lobstr";
var LobstrModule = class {
  constructor() {
    Object.defineProperty(this, "moduleType", {
      enumerable: true,
      configurable: true,
      writable: true,
      value: ModuleType.HOT_WALLET
    });
    Object.defineProperty(this, "productId", {
      enumerable: true,
      configurable: true,
      writable: true,
      value: LOBSTR_ID
    });
    Object.defineProperty(this, "productName", {
      enumerable: true,
      configurable: true,
      writable: true,
      value: "LOBSTR"
    });
    Object.defineProperty(this, "productUrl", {
      enumerable: true,
      configurable: true,
      writable: true,
      value: "https://lobstr.co"
    });
    Object.defineProperty(this, "productIcon", {
      enumerable: true,
      configurable: true,
      writable: true,
      value: "https://stellar.creit.tech/wallet-icons/lobstr.png"
    });
  }
  async runChecks() {
    if (!await this.isAvailable()) {
      throw new Error(`Lobstr is not connected`);
    }
  }
  async isAvailable() {
    return (0, import_signer_extension_api.isConnected)();
  }
  async getAddress() {
    try {
      await this.runChecks();
      const address = await (0, import_signer_extension_api.getPublicKey)();
      return { address };
    } catch (e) {
      throw parseError(e);
    }
  }
  async signTransaction(xdr, opts) {
    if (opts == null ? void 0 : opts.address) {
      console.warn(`Lobstr doesn't allow specifying what public key should sign the transaction, we skip the value`);
    }
    if (opts == null ? void 0 : opts.networkPassphrase) {
      console.warn(`Lobstr doesn't allow specifying the network that should be used, we skip the value`);
    }
    try {
      await this.runChecks();
      const signedTxXdr = await (0, import_signer_extension_api.signTransaction)(xdr);
      return { signedTxXdr };
    } catch (e) {
      throw parseError(e);
    }
  }
  async signMessage(message, opts) {
    if (opts == null ? void 0 : opts.address) {
      console.warn(`Lobstr doesn't allow specifying what public key should sign the transaction, we skip the value`);
    }
    if (opts == null ? void 0 : opts.networkPassphrase) {
      console.warn(`Lobstr doesn't allow specifying the network that should be used, we skip the value`);
    }
    try {
      await this.runChecks();
      const result = await (0, import_signer_extension_api.signMessage)(message);
      if (!result) {
        throw new Error("Signing message failed");
      }
      return result;
    } catch (e) {
      throw parseError(e);
    }
  }
  signAuthEntry() {
    return Promise.reject({
      code: -3,
      message: 'Lobstr does not support the "signAuthEntry" function'
    });
  }
  getNetwork() {
    return Promise.reject({
      code: -3,
      message: 'Lobstr does not support the "getNetwork" function'
    });
  }
};
export {
  LOBSTR_ID,
  LobstrModule
};
//# sourceMappingURL=@creit__tech_stellar-wallets-kit_modules_lobstr.js.map
